import Rules from "../class/Rules";
import Rule from "../class/Rule";
const rules = new Rules();
rules.setRules(new Rule("1", "3先(キャラセレOFF)"));
rules.setRules(new Rule("2", "5先(キャラセレOFF)"));
rules.setRules(new Rule("3", "3先(キャラセレON)"));
rules.setRules(new Rule("4", "5先(キャラセレON)"));
export { rules };
//# sourceMappingURL=rule.js.map